Perfect — now we got the **real root cause** 🔥

---

# 🚨 EXACT PROBLEM (100% confirmed)

Your cluster still has **active usage of `v1beta1` APIs**:

### From your output:

* `machine.openshift.io v1beta1`
* `cluster.x-k8s.io v1beta1`
* `metal3 remediation v1beta1`
* webhook paths using `v1beta1`

👉 These are **NOT just present — they are actively used by webhooks**

---

# ⚠️ WHY UPGRADE IS BLOCKED

Even though:

✔ admin-ack = true
❌ Upgradeable = False

Because:

👉 OpenShift detects **live usage of deprecated APIs**

**Important:**

* Having API available → OK
* **Using it in webhooks/resources → BLOCKER ❌**

---

# 🎯 ROOT CAUSE IN YOUR CLUSTER

From your data:

👉 These are causing block:

### 1️⃣ Machine API (very important)

```
machine.openshift.io/v1beta1
```

### 2️⃣ Metal3 / Baremetal

```
infrastructure.cluster.x-k8s.io/v1beta1
```

👉 These come from:

* Machine API Operator
* Baremetal Operator

---

# 🚀 FIX — STEP BY STEP (NO GUESSWORK)

## ✅ STEP 1 — Identify exact webhook names

Run:

```bash
oc get mutatingwebhookconfigurations
oc get validatingwebhookconfigurations
```

👉 Look for names like:

* machine-api
* metal3
* cluster-x

---

## ✅ STEP 2 — Inspect one webhook

Example:

```bash
oc get mutatingwebhookconfiguration <name> -o yaml
```

Check:

```yaml
admissionReviewVersions:
- v1beta1   ❌
```

---

## 🔥 STEP 3 — FIX (IMPORTANT)

### OPTION A (BEST) ✅

👉 Upgrade operator that owns webhook

But in disconnected cluster → not easy

---

### OPTION B (FASTEST FIX) ⚡

👉 Patch webhook to remove `v1beta1`

Example:

```bash
oc patch mutatingwebhookconfiguration <name> \
--type='json' \
-p='[{"op":"replace","path":"/webhooks/0/admissionReviewVersions","value":["v1"]}]'
```

👉 Do for ALL webhooks containing:

```
v1beta1
```

---

### ⚠️ If multiple webhooks inside

You may need to patch multiple indexes:

```bash
/webhooks/0/
/webhooks/1/
/webhooks/2/
```

---

## 🧨 STEP 4 — Restart CVO

```bash
oc delete pod -n openshift-cluster-version -l app=cluster-version-operator
```

---

## ✅ STEP 5 — Retry upgrade

```bash
oc adm upgrade \
--allow-explicit-upgrade \
--allow-upgrade-with-warnings \
--to-image=bastion.srib-lab-ocp.lan:8443/ocp4/openshift4/openshift/release-images@sha256:682e85bfe8034924e596b281ed8fefe4451e6f6c5bac07b5ec300443eeb23566
```

---

# 💡 IMPORTANT INSIGHT

👉 Your issue is **NOT mirror / checksum / IDMS**

👉 It is:

> ❌ Deprecated API still referenced by webhooks

---

# 🧠 WHY THIS HAPPENS

In disconnected clusters:

* Operators don’t auto-upgrade
* Old webhook configs remain
* CVO blocks upgrade for safety

---

# 🏁 FINAL RESULT AFTER FIX

You should see:

```bash
oc get clusterversion
```

```
Progressing=True  ✅
```

---

# 🚀 If you want fastest resolution

👉 Paste this:

```bash
oc get mutatingwebhookconfigurations
oc get validatingwebhookconfigurations
```

I’ll give you **exact patch commands per webhook (ready copy-paste)** ✅
