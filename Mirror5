Good — now I’ll **translate those Red Hat articles into exact actionable steps for YOUR cluster** (not generic theory).

---

# 📘 What those articles actually say (simplified)

From:

* [Preparing to upgrade to OpenShift 4.20](https://access.redhat.com/articles/7130599?utm_source=chatgpt.com)
* [Navigating Kubernetes API deprecations](https://access.redhat.com/articles/6955985?utm_source=chatgpt.com)

### 🔑 Core requirement

Before upgrade:

> ❗ You must **remove ALL usage of removed APIs**, not just acknowledge
> ❗ Otherwise upgrade is **BLOCKED by CVO** ([docs.okd.io][1])

---

# 🚨 YOUR EXACT ISSUE (mapped to article)

From your logs:

```text
v1beta1 admissionregistration APIs still used
```

From Red Hat:

👉 Removed in 4.20:

* `admissionregistration.k8s.io/v1beta1` ❌
* Must use: `v1` ✅ ([docs.redhat.com][2])

---

# ❌ Why your upgrade is stuck

Even though:

✔ admin-ack = true
❌ still blocked

Because:

> OpenShift detects **real usage of deprecated API (webhooks)**
> → blocks upgrade intentionally

---

# 🧠 IMPORTANT (from article 6955985)

Red Hat clearly says:

> OpenShift **cannot automatically detect all usage**
> → admin must manually fix it ([Red Hat Customer Portal][3])

👉 That is exactly your case

---

# 🚀 REAL SOLUTION (based on articles + your cluster)

## 🔥 STEP 1 — Identify actual objects (YOU ALREADY DID)

You found:

```text
mutatingwebhookconfigurations → v1beta1 ❌
validatingwebhookconfigurations → v1beta1 ❌
```

👉 This is the blocker

---

# 🔧 STEP 2 — FIX (MANDATORY)

## ✔ Convert webhook to v1

👉 Red Hat recommendation = **migrate API version**

So do:

```bash
oc get mutatingwebhookconfiguration <name> -o yaml
```

Find:

```yaml
admissionReviewVersions:
- v1beta1
```

---

## ✏️ PATCH TO v1

```bash
oc patch mutatingwebhookconfiguration <name> \
--type='json' \
-p='[{"op":"replace","path":"/webhooks/0/admissionReviewVersions","value":["v1"]}]'
```

---

## 🔁 Repeat for ALL webhooks

Do same for:

```bash
oc get validatingwebhookconfiguration
```

---

# ⚠️ IMPORTANT (from article)

> You must migrate ALL objects using removed APIs ([docs.okd.io][1])

👉 So:

* Not just 1 webhook
* ALL must be fixed

---

# 🧨 STEP 3 — Restart CVO

```bash
oc delete pod -n openshift-cluster-version -l app=cluster-version-operator
```

---

# 🔁 STEP 4 — Re-run upgrade

```bash
oc adm upgrade \
--allow-explicit-upgrade \
--allow-upgrade-with-warnings \
--to-image=bastion.srib-lab-ocp.lan:8443/ocp4/openshift4/openshift/release-images@sha256:682e85bfe8034924e596b281ed8fefe4451e6f6c5bac07b5ec300443eeb23566
```

---

# ✅ EXPECTED RESULT

```bash
oc get clusterversion
```

You should see:

```text
Progressing=True  ✅
```

---

# 🧠 EXTRA (VERY IMPORTANT)

From your cluster:

```text
machine.openshift.io
cluster.x-k8s.io
metal3
```

👉 These are managed by operators

💡 So BEST long-term fix:

* Upgrade operators (ODF / machine API)
* OR patch temporarily (your case)

---

# 🏁 FINAL CONCLUSION

👉 Based on Red Hat articles:

| Requirement       | Your status |
| ----------------- | ----------- |
| Admin ack         | ✅ Done      |
| Removed API usage | ❌ NOT fixed |
| Upgrade blocked   | ❌ Expected  |

---

# 🚀 Next (I can make it EASY)

Paste:

```bash
oc get mutatingwebhookconfigurations
oc get validatingwebhookconfigurations
```

👉 I will give **exact patch commands per webhook (copy-paste ready)**
(no trial/error, direct fix)

[1]: https://docs.okd.io/4.20/updating/preparing_for_updates/updating-cluster-prepare.html?utm_source=chatgpt.com "Preparing to update a cluster - OKD 4.20"
[2]: https://docs.redhat.com/es/documentation/openshift_container_platform/4.20/html/updating_clusters/preparing-to-update-a-cluster?utm_source=chatgpt.com "Chapter 2. Preparing to update a cluster"
[3]: https://access.redhat.com/articles/6955985?utm_source=chatgpt.com "Navigating Kubernetes API deprecations and removals"
