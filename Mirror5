Perfect 👍 — now we see the **exact issue clearly**, and your error actually tells the fix.

---

# 🚨 Why your patch FAILED

Error:

```bash
minAvailable and maxUnavailable cannot be both set
```

👉 Your PDB already has:

```bash
maxUnavailable: 0
```

👉 So adding `minAvailable` ❌ is invalid

---

# 🧠 Correct Fix (VERY IMPORTANT)

👉 You should **modify `maxUnavailable`**, NOT add `minAvailable`

---

# 🏆 DO THIS EXACTLY

## ✅ Step 1: Patch OSD PDB correctly

```bash
oc patch pdb rook-ceph-osd-rack-rack0 -n openshift-storage \
--type merge -p '{"spec":{"maxUnavailable":1}}'

oc patch pdb rook-ceph-osd-rack-rack2 -n openshift-storage \
--type merge -p '{"spec":{"maxUnavailable":1}}'
```

---

## 🔍 Step 2: Verify

```bash
oc get pdb -n openshift-storage
```

👉 You should now see:

```bash
MAX UNAVAILABLE = 1
ALLOWED DISRUPTIONS >= 1 ✅
```

---

# 🚀 Step 3: Retry drain

```bash
oc adm drain ocp-cp-3.lab1.srib-lab-ocp.lan \
--ignore-daemonsets --delete-emptydir-data --force --grace-period=0
```

👉 This time:
✔ OSD pod will be evicted
✔ Drain will succeed

---

# 🔄 Step 4: Watch upgrade resume

```bash
watch oc get mcp
```

👉 Expect:

```bash
master → UPDATED=True
UPDATING=False
```

---

# ⚠️ Why this works

Before:

```bash
maxUnavailable = 0 → no eviction ❌
```

After:

```bash
maxUnavailable = 1 → eviction allowed ✅
```

---

# 💡 Small but critical concept

| Setting          | Meaning                |
| ---------------- | ---------------------- |
| maxUnavailable=0 | Block ALL evictions ❌  |
| maxUnavailable=1 | Allow 1 pod eviction ✅ |

---

# 🏁 Final Answer

👉 ❌ Don’t use `minAvailable`
👉 ✅ Update `maxUnavailable`

---

# 👍 You’re literally at final unblock step

👉 After this:

* Drain will succeed
* Node updates
* MachineConfig recovers
* Upgrade continues

---

If still stuck after patch, paste:

```bash
oc get pdb -n openshift-storage
```

I’ll finish it completely 👍
