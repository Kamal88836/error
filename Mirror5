Perfect 👍 — now we have the **exact blocker identified**.

---

# 🚨 FINAL ROOT CAUSE

👉 This pod is blocking everything:

```bash
openshift-storage/rook-ceph-osd-1-b944c5b94-jsbtn
```

👉 Error:

```bash
Cannot evict pod as it would violate the pod's disruption budget
```

💥 So:

* Drain is failing ❌
* Node `ocp-cp-3` stuck ❌
* MachineConfigPool stuck ❌
* Upgrade stuck ❌

---

# 🧠 Why only this pod?

👉 All other pods:

* DaemonSets → ignored ✅
* Control plane pods → handled by OpenShift ✅

👉 BUT:

* Ceph OSD pod = **stateful + protected by PDB** ❌

---

# 🏆 FINAL FIX (SAFE FOR CEPH)

We will **temporarily relax PDB properly**

---

## ✅ Step 1: Check exact PDB

```bash
oc get pdb -n openshift-storage
```

👉 Find which PDB is controlling this pod
(It will be something like `rook-ceph-osd-rack-*`)

---

## ✅ Step 2: Patch PDB (IMPORTANT)

👉 Run for ALL OSD PDBs:

```bash
oc patch pdb rook-ceph-osd-rack-rack0 -n openshift-storage \
-p '{"spec":{"minAvailable":0}}'

oc patch pdb rook-ceph-osd-rack-rack2 -n openshift-storage \
-p '{"spec":{"minAvailable":0}}'
```

---

## 🔍 Step 3: Verify

```bash
oc get pdb -n openshift-storage
```

👉 Now you should see:

```bash
ALLOWED DISRUPTIONS >= 1 ✅
```

---

## 🚀 Step 4: Retry drain

```bash
oc adm drain ocp-cp-3.lab1.srib-lab-ocp.lan \
--ignore-daemonsets --delete-emptydir-data --force --grace-period=0
```

👉 This time:
✔ OSD pod will be evicted
✔ Node will proceed to reboot

---

# 🔄 Step 5: Watch recovery

```bash
watch oc get nodes
```

👉 Expect:

```text
ocp-cp-3 → NotReady → reboot → Ready
```

---

```bash
watch oc get mcp
```

👉 Expect:

```text
master → UPDATED=True
UPDATING=False
```

---

# ⚠️ VERY IMPORTANT

👉 This is SAFE because:

* You still have other OSDs on other nodes
* Only 1 disruption allowed
* Ceph keeps quorum

---

# 🧠 Why earlier didn’t work

You tried:

* ❌ wrong PDB name
* ❌ didn’t change minAvailable

👉 So:

```bash
ALLOWED DISRUPTIONS = 0 ❌
```

---

# 🏁 After upgrade completes

👉 Restore back (optional):

```bash
oc edit pdb rook-ceph-osd-rack-rack0 -n openshift-storage
```

---

# 👍 Final Summary

👉 One OSD pod blocking drain
👉 PDB preventing eviction
👉 Patch PDB → allow disruption
👉 Drain succeeds → upgrade continues

---

# 🚀 You are at LAST STEP

👉 After this → upgrade will continue automatically

---

If still stuck after patch, paste:

```bash
oc get pdb -n openshift-storage
```

I’ll finish it with you 👍
