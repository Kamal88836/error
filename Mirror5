Perfect 👍 — this output tells the **exact situation clearly**. Let’s break it and fix it properly.

---

# 🚨 ROOT CAUSE (100% clear now)

From your logs:

```text
MachineConfigPool master is not ready
ready 2, updated 2, unavailable 1
```

👉 And:

```bash
oc get nodes
ocp-cp-3 → Ready,SchedulingDisabled
```

👉 So:

💥 **Node `ocp-cp-3` is stuck in drain state**
💥 That’s why **machine-config is degraded**
💥 That’s why **upgrade is blocked**

---

# 🧠 What already happened

* Node was cordoned ✔️
* Drain attempted ❌ (blocked by Ceph earlier)
* Node never completed update ❌
* MCO waiting forever ⏳

---

# 🏆 FIX (SAFE & CORRECT)

We will **complete the stuck node update**

---

## ✅ Step 1: Check pods still on node

```bash
oc get pods -A -o wide | grep ocp-cp-3
```

👉 You’ll see Ceph or system pods still there

---

## ✅ Step 2: Force drain AGAIN (now PDB adjusted)

```bash
oc adm drain ocp-cp-3.lab1.srib-lab-ocp.lan \
--ignore-daemonsets --delete-emptydir-data --force --grace-period=0
```

👉 This time it should succeed (since you handled PDB)

---

## ❗ If STILL blocked

Then manually delete remaining pods:

```bash
oc get pods -A -o wide | grep ocp-cp-3
```

Then delete problematic ones:

```bash
oc delete pod <pod> -n <namespace> --force --grace-period=0
```

---

## ✅ Step 3: Watch node

```bash
watch oc get nodes
```

👉 Expect:

```text
ocp-cp-3 → NotReady → reboot → Ready
```

---

## ✅ Step 4: Watch MCP

```bash
watch oc get mcp
```

👉 Wait for:

```text
master → UPDATED=True
UPDATING=False
```

---

## ✅ Step 5: Verify machine-config

```bash
oc get co machine-config
```

👉 Should become:

```text
Available=True
Degraded=False
```

---

# 🚨 Additional Issues (you also have)

From ClusterVersion:

```text
authentication degraded
openshift-apiserver degraded
insights not available
```

---

## 🔧 Fix insights (safe in airgap)

```bash
oc delete deployment insights-operator -n openshift-insights
oc delete co insights
```

---

## 🔍 Check authentication

```bash
oc get co authentication
oc describe co authentication
```

👉 Usually auto-recovers after MCO fixed

---

# 💡 Important Understanding

👉 Your upgrade is NOT broken
👉 It is **waiting for 1 node (ocp-cp-3)**

Once that node updates:
💥 Everything will recover automatically

---

# 🏁 FINAL PLAN

1. Force drain node ✅
2. Ensure reboot happens ✅
3. Wait for MCP complete ✅
4. Machine-config becomes healthy ✅
5. Other operators recover ✅

---

# 👍 You’re very close

👉 This is **last real blocker** in your upgrade

---

If after drain still stuck, paste:

```bash
oc get pods -A -o wide | grep ocp-cp-3
```

I’ll tell exactly which pod is blocking 👍
