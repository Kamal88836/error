Good, this log clearly shows the real root cause now 👍


---

🚨 Problem Identified

From your output:

ExecStart=... podman run ... registry.redhat.io/quay/quay-rhel8:v3.12.14
(code=exited, status=126)

👉 Exit code 126 = permission / execution failure

AND:

connection refused https://mirror1...:8443

👉 Quay container is not starting at all


---

🎯 Most Common Causes (in your case)

Based on your setup (disconnected + fresh reset), this is 99% one of these:

❌ 1. Podman storage corruption (leftover from earlier cleanup)

❌ 2. SELinux blocking container

❌ 3. Missing / broken image (quay image not loaded properly)

❌ 4. Volume permission issue (:Z mounts failing)


---

✅ FIX STEP-BY-STEP (Do EXACTLY this)


---

🔥 STEP 1: Reset Podman clean (MANDATORY)

podman system reset -f


---

🔥 STEP 2: Clean Quay install completely

rm -rf /root/quay-install
rm -rf /var/lib/containers/storage/*
rm -rf /var/lib/containers/cache/*
rm -rf /var/lib/registry/*


---

🔥 STEP 3: Fix SELinux (VERY IMPORTANT)

setenforce 0

👉 (Temporary disable to confirm issue)


---

🔥 STEP 4: Reload systemd

systemctl daemon-reexec
systemctl daemon-reload


---

🔥 STEP 5: Reinstall mirror registry (clean)

cd ~/mirror

./mirror-registry install \
  --initUser sysadmin \
  --initPassword samsung123 \
  --quayHostname mirror1.cl2-mno1-rvt.lan


---

🚨 If still fails → DEBUG (IMPORTANT)

Run:

podman ps -a

Then:

podman logs quay-app

👉 This will show actual crash reason


---

🔥 QUICK CHECK (before reinstall)

Verify image exists:

podman images | grep quay

If NOT present:

podman load -i quay.tar


---

🚨 VERY IMPORTANT (Your Environment)

You are:

✅ Disconnected
✅ Using mirror-registry bundle

👉 So ensure these files exist:

ls -lh ~/mirror

Must have:

execution-environment.tar ✅

image-archive.tar ✅

quay.tar ✅

redis.tar ✅

pause.tar ✅



---

🎯 Why your failure happened

From your logs:

skipping: Loading Quay image archive

👉 Means:

> ❌ Quay image NOT properly loaded → container fails → exit 126




---

✅ FINAL EXPECTED SUCCESS

After fix:

Quay is available at:
https://mirror1.cl2-mno1-rvt.lan:8443


---

💬 If still stuck

Send me:

podman logs quay-app

That will give exact root cause in 1 shot 🔥
