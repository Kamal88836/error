Got it 👍 — you **don’t want full catalog (size concern)**, but still need to fix missing ODF images.

So we’ll **keep your YAML mostly same** and just add the **missing dependency explicitly**.

---

# 🔥 Root Issue Recap (important)

Missing image:

```text
odf-cloudnative-pg-rhel9-operator
```

👉 This is **NOT included automatically** even though you added ODF packages.

---

# ✅ FIXED YAML (minimal change to YOUR config)

👉 I only added the required missing image in `additionalImages`

```yaml
apiVersion: mirror.openshift.io/v2alpha1
kind: ImageSetConfiguration

archiveSize: 4

mirror:
  platform:
    graph: true
    channels:
      - name: stable-4.20
        minVersion: 4.20.14
        maxVersion: 4.20.14

  operators:
    - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.20
      packages:

        # ODF FULL STACK
        - name: odf-operator
          channels:
            - name: stable-4.20

        - name: ocs-operator
          channels:
            - name: stable-4.20

        - name: ocs-client-operator
          channels:
            - name: stable-4.20

        - name: mcg-operator
          channels:
            - name: stable-4.20

        - name: rook-ceph-operator
          channels:
            - name: stable-4.20

        - name: cephcsi-operator
          channels:
            - name: stable-4.20

        - name: odf-csi-addons-operator
          channels:
            - name: stable-4.20

        - name: odf-prometheus-operator
          channels:
            - name: stable-4.20

        - name: odf-dependencies
          channels:
            - name: stable-4.20

        - name: recipe
          channels:
            - name: stable-4.20

        # STORAGE
        - name: lvms-operator
          channels:
            - name: stable-4.20

        - name: local-storage-operator
          channels:
            - name: stable

        # NETWORK
        - name: kubernetes-nmstate-operator
          channels:
            - name: stable

        - name: sriov-network-operator
          channels:
            - name: stable

        - name: ptp-operator
          channels:
            - name: stable

        # SECURITY / OPTIONAL
        - name: openshift-cert-manager-operator
        - name: cluster-observability-operator
        - name: kubernetes-image-registry-operator

  additionalImages:
    # CORE RELEASE
    - name: quay.io/openshift-release-dev/ocp-release:4.20.14-x86_64

    # 🔥 CRITICAL FIX (THIS WAS MISSING)
    - name: registry.redhat.io/odf4/odf-cloudnative-pg-rhel9-operator:latest

    # BASE IMAGES
    - name: registry.redhat.io/ubi9/ubi:latest
    - name: registry.redhat.io/ubi9/ubi-minimal:latest
    - name: registry.redhat.io/ubi9/ubi-init:latest

    # SUPPORT
    - name: registry.redhat.io/rhel9/support-tools:latest

  helm: {}
```

---

# ⚠️ Important note (READ THIS)

👉 Your cluster is using **digest**:

```text
@sha256:3c8677b5d1adcd8ff300dce224c63745eab2397254a74940359f6ac14a81ddc7
```

But we added:

```yaml
:latest
```

👉 This usually works because:

* `oc mirror` pulls full manifest list
* digest will match internally

---

# 🚀 If still fails (rare case)

Then use exact digest:

```yaml
- name: registry.redhat.io/odf4/odf-cloudnative-pg-rhel9-operator@sha256:3c8677b5d1adcd8ff300dce224c63745eab2397254a74940359f6ac14a81ddc7
```

---

# 🚀 Next Steps

1️⃣ Run mirror again

```bash
oc mirror -c imageset-config.yaml file:///root/mirror_4_20/ --v2
```

2️⃣ Push to registry

3️⃣ Restart pod

```bash
oc delete pod -n openshift-storage -l app.kubernetes.io/name=cloudnative-pg
```

---

# 🎯 Expected Result

```text
cnpg-controller-manager → Running ✅
mcg-operator → Succeeded ✅
Upgrade resumes 🚀
```

---

# 💬 Honest advice (important)

👉 Your approach (minimal mirror) works BUT:

* You may hit **more missing images later**
* ODF has many hidden dependencies

👉 If you face next failure → better switch to full catalog

---

If you want, I can help you:
✅ Find ALL required ODF images (fully optimized small mirror)
✅ So you avoid trial-and-error

Just tell me 👍
