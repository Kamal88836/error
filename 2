Now we can clearly see the remaining issue.

odf-dependencies subscription exists, but:

no new CSV created

no installplan for 4.19

ODF operator remains in Installing


This means OLM resolver is still pinned to old dependency graph.

The stale object now is likely the old InstallPlan:

install-g7dq2   odf-dependencies.v4.18.14-rhodf

and possibly stale CSVs still owned by previous resolution chain.

Next safe recovery step:

Step 1 — Delete stale installplan for odf-dependencies

oc delete installplan install-g7dq2 -n openshift-storage

Do NOT delete:

install-rkpq9

odf-operator CSV

StorageCluster



---

Step 2 — Delete odf-dependencies subscription

oc delete sub odf-dependencies -n openshift-storage


---

Step 3 — Restart ODF operator again

oc delete pod -n openshift-storage -l control-plane=controller-manager


---

Step 4 — Watch recreation

watch oc get sub,csv,installplan -n openshift-storage

Expected:

odf-dependencies subscription recreated

NEW installplan generated

NEW odf-dependencies.v4.19.12-rhodf

then remaining subscriptions generated automatically


You are now fixing the last stale resolver chain from old 4.18 dependency metadata.
