Follow this exact recovery flow.

Your storage is healthy, so we only repair the stuck OLM/ODF dependency chain.

Step 1 — Verify no installplans pending

oc get installplan -A

If there are no new 4.19 installplans, continue.


---

Step 2 — Backup current subscriptions

mkdir -p /root/odf-backup

oc get sub -n openshift-storage -o yaml > /root/odf-backup/odf-subs.yaml


---

Step 3 — Delete ONLY stale dependent subscriptions

DO NOT delete:

StorageCluster

CephCluster

CSVs

odf-operator subscription


Delete only these:

oc delete sub -n openshift-storage \
rook-ceph-operator-stable-4.18-redhat-operator-index-openshift-marketplace \
cephcsi-operator-stable-4.18-redhat-operator-index-openshift-marketplace \
mcg-operator-stable-4.18-redhat-operator-index-openshift-marketplace \
ocs-client-operator-stable-4.18-redhat-operator-index-openshift-marketplace \
ocs-operator-stable-4.18-redhat-operator-index-openshift-marketplace \
odf-csi-addons-operator-stable-4.18-redhat-operator-index-openshift-marketplace \
odf-prometheus-operator-stable-4.18-redhat-operator-index-openshift-marketplace \
recipe-stable-4.18-redhat-operator-index-openshift-marketplace \
odf-dependencies


---

Step 4 — Restart OLM operators

oc delete pod -n openshift-operator-lifecycle-manager -l app=olm-operator

oc delete pod -n openshift-operator-lifecycle-manager -l app=catalog-operator

Wait 2-3 minutes.


---

Step 5 — Restart ODF operator

oc delete pod -n openshift-storage -l app.kubernetes.io/name=odf-operator-controller-manager

If label does not work:

oc delete pod -n openshift-storage odf-operator-controller-manager-79cdcbd66f-vdk9t


---

Step 6 — Watch recreation

Watch subscriptions:

watch oc get sub -n openshift-storage

Expected:

subscriptions recreated automatically

installplans generated

CSVs move to 4.19.12



---

Step 7 — Watch CSV upgrades

watch oc get csv -n openshift-storage

Expected eventually:

rook-ceph-operator.v4.19.12-rhodf
ocs-operator.v4.19.12-rhodf
mcg-operator.v4.19.12-rhodf
recipe.v4.19.12-rhodf
odf-dependencies.v4.19.12-rhodf


---

Step 8 — Verify StorageCluster version

oc get storagecluster -n openshift-storage

Expected:

VERSION
4.19.12


---

Step 9 — Final validation

oc get co

and:

ceph status

inside toolbox if available.


---

Very important:

Do NOT delete CSVs manually

Do NOT delete StorageCluster

Do NOT delete CephCluster

Do NOT delete namespace


Your issue is only stale OLM subscription resolution after disconnected upgrade reconciliation.
