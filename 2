Now the situation is much clearer.

Your odf-dependencies.v4.18.17-rhodf got recreated successfully, but all dependent operators are stuck in Pending.

The key error is:

Service account does not exist
Name: ocs-operator

and:

one or more requirements couldn't be found

This means the CSVs were recreated, but their install resources were not fully restored after you deleted the CSVs manually.

Right now you have an inconsistent OLM state:

CSV exists

but deployment/serviceaccount/RBAC objects are missing

therefore CSV remains Pending


This is no longer a mirror/catalog issue.

The immediate problem is that these operators lost their generated install resources when the CSVs were deleted.

Especially:

serviceaccounts

deployments

RBAC

webhooks


Since:

odf-dependencies.v4.18.17-rhodf = Succeeded

the mirror/catalog side is actually working now.

The safest recovery now is to force OLM to recreate the operator install resources cleanly.

Run this carefully.

First remove the broken Pending CSVs only:

oc delete csv \
cephcsi-operator.v4.18.17-rhodf \
mcg-operator.v4.18.17-rhodf \
ocs-client-operator.v4.18.17-rhodf \
ocs-operator.v4.18.17-rhodf \
odf-csi-addons-operator.v4.18.17-rhodf \
odf-prometheus-operator.v4.18.17-rhodf \
-n openshift-storage

DO NOT delete:

odf-dependencies

odf-operator


Then delete installplans:

oc delete installplan --all -n openshift-storage

Then restart catalog + olm operators:

oc delete pod -n openshift-operator-lifecycle-manager -l app=catalog-operator

oc delete pod -n openshift-operator-lifecycle-manager -l app=olm-operator

Then monitor:

watch 'oc get csv,installplan,pods -n openshift-storage'

You should see:

new installplans

CSVs recreated

deployments/serviceaccounts recreated properly

Pending → Installing → Succeeded


Important: DO NOT manually create serviceaccounts or deployments. OLM owns them.

Also: DO NOT delete odf-dependencies.v4.18.17-rhodf because it is now the anchor that restored dependency resolution.
