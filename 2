odf-operator is now healthy and Succeeded, which is good.

But the dependency operators are still stuck at 4.18.17 because OLM has not generated new InstallPlans for them yet.

Next check the subscription status and current CSV mapping:

oc describe sub odf-dependencies -n openshift-storage

Look specifically for:

CurrentCSV

InstallPlan

State


Most likely it is still pinned or resolution failed.

Then check catalog resolution errors:

oc get installplan -n openshift-storage

and:

oc logs -n openshift-operator-lifecycle-manager deploy/catalog-operator --tail=100 | grep -i openshift-storage

From your current state, the probable issue is:

old 4.18 CSVs are still owning APIs/resources

OLM dependency resolver is blocked


You can safely force regeneration of the dependency chain now.

Delete ONLY the old dependency CSVs (NOT odf-operator):

oc delete csv \
cephcsi-operator.v4.18.17-rhodf \
mcg-operator.v4.18.17-rhodf \
ocs-client-operator.v4.18.17-rhodf \
ocs-operator.v4.18.17-rhodf \
odf-csi-addons-operator.v4.18.17-rhodf \
odf-prometheus-operator.v4.18.17-rhodf \
recipe.v4.18.17-rhodf \
rook-ceph-operator.v4.18.17-rhodf \
-n openshift-storage

Do NOT delete:

odf-operator.v4.19.12-rhodf

StorageCluster

CephCluster

pods/PVCs


After deletion, OLM should immediately recreate InstallPlans and pull 4.19.12 dependency CSVs.

Then monitor:

watch -n 5 'oc get csv -n openshift-storage'

Expected final state:

CSV	Version

odf-operator	4.19.12
rook-ceph-operator	4.19.12
ocs-operator	4.19.12
mcg-operator	4.19.12
cephcsi-operator	4.19.12


Once all are Succeeded, the cluster is fully aligned again.
