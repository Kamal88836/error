This is actually good progress.

Now:

odf-operator.v4.19.12-rhodf = Succeeded

only supporting operators remain on 4.18.17

ODF recreated only:

odf-operator

odf-dependencies



This means:

odf-operator reconciliation is working now

But:

odf-dependencies operator is not creating the dependent subscriptions

That usually means one of these:

odf-dependencies CSV/controller stuck

stale CSV ownership

dependency resolution cache stale


Now do this next.

Step 1 — Delete old odf-dependencies CSV ONLY

oc delete csv odf-dependencies.v4.18.17-rhodf -n openshift-storage

DO NOT delete:

odf-operator CSV

rook-ceph CSV

storagecluster



---

Step 2 — Restart ODF operator again

oc delete pod -n openshift-storage -l control-plane=controller-manager

If label fails:

oc delete pod -n openshift-storage $(oc get pod -n openshift-storage | grep odf-operator-controller-manager | awk '{print $1}')


---

Step 3 — Watch subscriptions

watch oc get sub -n openshift-storage

Expected: new subscriptions should reappear:

rook-ceph

ocs-operator

mcg

recipe

cephcsi

etc



---

Step 4 — Watch installplans

watch oc get installplan -n openshift-storage


---

Step 5 — Watch CSV upgrades

watch oc get csv -n openshift-storage

You should eventually see:

*.v4.19.12-rhodf

The cluster is no longer broken. Now it is mainly:

stale dependency ownership metadata

Very important: Do NOT manually recreate subscriptions yet. Let odf-operator reconcile first after old dependency CSV removal.
