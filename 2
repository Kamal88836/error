This confirms the catalog is correct and complete.

Important findings:

stable-4.19 exists

rook-ceph-operator.v4.19.12-rhodf exists

default channel is stable-4.19

bundle metadata is present

related images are present in catalog metadata


So the issue is now purely OLM resolution/reconciliation.

The critical mismatch is:

Object	Current

Subscription channel	stable-4.19
Package default	stable-4.19
Available CSV	4.19.12
Installed CSV	4.18.17
InstallPlan	not generated


This usually happens when OLM still holds old resolution state for the subscription chain.

Now the cleanest supported recovery is:

1. Backup current subscriptions YAML


2. Delete ONLY the child subscriptions


3. Let odf-operator recreate them automatically



Because these subscriptions are operator-managed:

olm.managed: "true"
ownerReferences:
- kind: Subscription
  name: odf-operator

The umbrella odf-operator should recreate them correctly with new resolution state.

Do this carefully.

First backup:

oc get sub -n openshift-storage -o yaml > /tmp/odf-subs-backup.yaml

Then delete ONE child subscription first for testing:

oc delete sub -n openshift-storage rook-ceph-operator-stable-4.18-redhat-operator-index-openshift-marketplace

Then watch:

oc get sub -n openshift-storage -w

and:

oc get installplan -A -w

Expected behavior:

odf-operator recreates subscription

new subscription uses stable-4.19 cleanly

new installplan generated

CSV upgrades toward 4.19.12


If that works, repeat for remaining child subscriptions.

Do NOT delete:

odf-operator subscription

StorageCluster

CSVs

InstallPlans


Only delete the child subscriptions managed by odf-operator.
