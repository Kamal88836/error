Your catalog sources themselves are healthy.

The important part:

Last Observed State: READY

for both:

certified-operator-index

redhat-operator-index


So OLM can talk to the catalog successfully.

Now the remaining issue is almost certainly:

missing ODF 4.19 bundle/package inside mirrored index OR

bundle extraction image pull failure from harbor


Next checks:

Verify package manifests

Run:

oc get packagemanifest | grep -E 'odf|ocs|ceph|mcg'

Then especially:

oc describe packagemanifest odf-dependencies

You need to confirm:

package exists

channel stable-4.19 exists

currentCSV = odf-dependencies.v4.19.12-rhodf



---

Very important: inspect catalog pod logs

Since catalog is READY but unpack fails, check for missing bundles.

Run:

oc logs -n openshift-marketplace redhat-operator-index-ljgv2 | grep -i odf

and:

oc logs -n openshift-marketplace redhat-operator-index-ljgv2 | grep -Ei 'error|fail|missing|manifest'

You are looking for:

manifest unknown

image not found

package missing

bundle missing



---

Most likely issue

You mirrored:

redhat-operator-index:v4.19

but the actual bundle image:

odf-operator-bundle-rhel9:v4.19.12

or dependent bundle images were NOT mirrored.

In disconnected ODF upgrades, mirroring only the index image is NOT enough.

You must mirror:

index image

all related ODF bundles

related operands



---

Check whether bundle image exists in Harbor

From bastion:

skopeo inspect docker://nwharbor.sec.samsung.net/team-ocp/ocp/redhat/odf4/odf-operator-bundle-rhel9:v4.19.12

Also check:

skopeo inspect docker://nwharbor.sec.samsung.net/team-ocp/ocp/redhat/odf4/odf-rhel9-operator:v4.19.12

If these fail: that is the root cause.


---

Another strong clue

Your unpack jobs are NOT even appearing:

oc get jobs -A | grep unpack
→ nothing

That typically means: catalog resolution succeeds partially, but required bundle metadata/images are missing from the mirrored catalog content.


---

Likely fix

You probably need to re-mirror ODF 4.19 content completely using either:

oc mirror OR

oc adm catalog mirror


including:

redhat-operator-index:v4.19

all referenced ODF bundles/images


Your current mirror appears incomplete for ODF 4.19.12.
