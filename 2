Now the root cause is much clearer.

Your odf-operator.v4.19.12-rhodf is no longer blocked by dependencies.

The actual failure is:

> deployment "odf-operator-controller-manager" not available: Deployment does not have minimum availability



So OLM is repeatedly reinstalling because the deployment is not becoming Ready consistently.

Check the deployment and pod state now.

Run:

oc get deploy odf-operator-controller-manager -n openshift-storage

Then:

oc describe deploy odf-operator-controller-manager -n openshift-storage

And:

oc get po -n openshift-storage -l control-plane=controller-manager -o wide

Most likely one of these is happening:

pod crashlooping

readiness probe failing

old ReplicaSet conflict

webhook secret missing

image pull issue in disconnected env

deployment unavailable because replica never becomes Ready


Especially important in disconnected environments: the CSV upgraded to 4.19 but dependent images may not exist in your mirrored registry / ICSP / IDMS.

Check pod events carefully.

Also verify:

oc logs -n openshift-storage deploy/odf-operator-controller-manager --all-containers --tail=100

And:

oc get events -n openshift-storage --sort-by=.metadata.creationTimestamp | tail -40

Your storage itself is safe right now because:

StorageCluster = Ready

Ceph pods healthy

no data path issue


This is now purely an operator deployment availability issue, not storage corruption.
