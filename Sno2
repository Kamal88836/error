[root@bastion-sno mirror-openshift-install]# export PRODUCT_REPO="openshift-release-dev/ocp-release"
[root@bastion-sno mirror-openshift-install]# export RELEASE_NAME="release"
[root@bastion-sno mirror-openshift-install]# echo ${PRODUCT_REPO}
echo ${RELEASE_NAME}
echo ${OCP_RELEASE}
echo ${ARCHITECTURE}
openshift-release-dev/ocp-release
release
4.14.46
x86_64
[root@bastion-sno mirror-openshift-install]# oc adm release mirror \
  -a ${LOCAL_SECRET_JSON} \
  --from=quay.io/${PRODUCT_REPO}/${RELEASE_NAME}:${OCP_RELEASE}-${ARCHITECTURE} \
  --to=${LOCAL_REGISTRY}/${LOCAL_REPOSITORY} \
  --to-release-image=${LOCAL_REGISTRY}/${LOCAL_REPOSITORY}:${OCP_RELEASE}-${ARCHITECTURE}
Flag --print-mirror-instructions's value 'icsp' has been deprecated. Use 'idms' instead to allow the printing of instructions for ImageDigestSources and ImageDigestMirrorSet.
error: unable to retrieve release image info: unable to read image quay.io/openshift-release-dev/ocp-release/release:4.14.46-x86_64: unauthorized: access to the requested resource is not authorized
[root@bastion-sno mirror-openshift-install]#
