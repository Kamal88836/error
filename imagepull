Good — this output tells us something very important 👇

---

# 🚨 Your problem is STILL NOT FIXED

Look carefully at this line:

```text
bastion.../ocp4/openshift4/openshift4/ose-local-storage...
                           ❌ duplicated openshift4
```

👉 This means:

❌ **Old IDMS is STILL active somewhere**
❌ Cluster is still using **wrong mirror path**

---

# 🎯 What should be happening

From your new mirror config, it should be:

```text
bastion.../openshift4/ose-local-storage...
```

NOT:

```text
.../ocp4/openshift4/openshift4/... ❌
```

---

# 🔍 Root Cause

👉 Even after your changes:

* Old IDMS still exists **OR**
* Nodes have not picked new config yet

---

# 🚀 Fix it properly (do EXACT steps)

## ✅ Step 1: Check current IDMS

```bash
oc get imagedigestmirrorset -o yaml
```

👉 Look for ANY entry containing:

```
ocp4/openshift4/openshift4 ❌
```

---

## ✅ Step 2: Clean EVERYTHING (important)

```bash
oc delete imagedigestmirrorset --all
oc delete imagetagmirrorset --all
```

---

## ✅ Step 3: Re-apply ONLY new mirror configs

```bash
oc apply -f idms-oc-mirror.yaml
oc apply -f itms-oc-mirror.yaml
```

---

## ✅ Step 4: Verify (VERY IMPORTANT)

```bash
oc get imagedigestmirrorset -o yaml
```

👉 It should show ONLY:

```text
bastion.srib-lab-ocp.lan:8443/openshift4
```

👉 NOT:

```text
ocp4/openshift4/openshift4 ❌
```

---

## ✅ Step 5: Force node to refresh image config

Pick one master:

```bash
oc debug node/ocp-cp-3.lab1.srib-lab-ocp.lan
chroot /host
systemctl restart crio
exit
```

👉 Do for all masters (important)

---

## ✅ Step 6: Delete failed pods again

```bash
oc get pods -A | grep -i pull | awk '{print $1, $2}' | while read ns pod; do
  oc delete pod $pod -n $ns
done
```

---

# 🔥 Why restart CRI-O?

👉 Nodes cache image pull config
👉 Without restart, they may still use old mirror paths

---

# 🎯 Expected result after fix

✔ No more `openshift4/openshift4`
✔ Image pulls from correct path
✔ Pods start running
✔ MCP progresses

---

# ⚠️ Important Insight

👉 Your mirror is now correct
👉 But cluster is still using **old config (cached / leftover IDMS)**

---

# 👍 Final Answer

👉 Problem is NOT mirror now
👉 Problem is **old mirror config still active**

---

If you want, paste:

```bash
oc get imagedigestmirrorset -o yaml
```

I’ll confirm 100% clean state 👍
