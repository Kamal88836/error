Good, now we have clear root cause 👇


---

🚨 Final Root Cause

👉 On node master1:

ovnkube-node pod = CrashLoopBackOff

Failing container = ovnkube-controller

Result:

ovn-cni-server.sock → exists BUT not responding
→ connection refused


💥 That’s why:

Multus fails

Pod sandbox fails

All CNF pods stuck in PodInitializing



---

⚠️ Important Observation (VERY KEY)

From node:

Failed Units:
  chronyd.service
  NetworkManager-wait-online.service

👉 This is NOT normal in OpenShift

💥 This strongly indicates:

> ❌ Node network stack is unstable / not fully initialized



And OVN depends heavily on:

NetworkManager

OVS (Open vSwitch)

proper node networking



---

🎯 Actual Problem Chain

NetworkManager issue
   ↓
OVS / OVN not healthy
   ↓
ovnkube-controller crashes
   ↓
ovn-cni-server not responding
   ↓
CNI add fails
   ↓
Pods stuck Pending


---

🔍 Next Step (CRITICAL DEBUG)

We need exact reason why ovnkube-controller is crashing.

👉 Run this:

oc logs ovnkube-node-gn2hz -n openshift-ovn-kubernetes -c ovnkube-controller

AND also:

oc logs ovnkube-node-gn2hz -n openshift-ovn-kubernetes -c ovn-controller


---

🛠️ Immediate Recovery (Try This First)

✅ 1. Restart OVN pod cleanly

oc delete pod ovnkube-node-gn2hz -n openshift-ovn-kubernetes

👉 Wait and check:

oc get pods -n openshift-ovn-kubernetes -o wide | grep master1


---

✅ 2. Check OVS on node

SSH to node:

sudo systemctl status openvswitch

If not active:

sudo systemctl restart openvswitch


---

✅ 3. Fix NetworkManager issue

Check:

sudo systemctl status NetworkManager

If not healthy:

sudo systemctl restart NetworkManager


---

⚠️ 4. chronyd failure (important for CNF)

sudo systemctl status chronyd

If failed:

sudo systemctl restart chronyd

👉 Time sync issues can break OVN/OVS TLS & cluster comms


---

✅ 5. Verify OVN socket again

sudo ls -l /var/run/ovn-kubernetes/cni/

Then test:

sudo ss -lx | grep ovn

👉 Socket should be actively listening


---

💡 What I Suspect (Based on your logs)

Most likely one of these:

🔴 1. OVS is down / broken

common after reboot or NIC issue


🔴 2. NetworkManager stuck

you already saw:

NetworkManager-wait-online.service FAILED


🔴 3. Time sync issue

chronyd failed → affects cluster certs / OVN



---

✅ Expected Healthy State

After fix:

oc get pods -n openshift-ovn-kubernetes

👉 Should be:

ovnkube-node-xxxxx   8/8 Running


---

🚀 Once fixed

Immediately your CNF pods will:

Pending → ContainerCreating → Running


---

👉 If still not resolved

Share this output:

oc logs ovnkube-node-gn2hz -n openshift-ovn-kubernetes -c ovnkube-controller

That will give exact crash reason (100% root cause) and I’ll pinpoint it precisely.
