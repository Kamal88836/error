controller-1:~$ source /etc/platform/openrc
Openstack Admin credentials can only be loaded from the active controller.
controller-1:~$ system application-list
You must provide a password via either --os-password or via env[OS_PASSWORD]
controller-1:~$
controller-1:~$ system host-list
You must provide a password via either --os-password or via env[OS_PASSWORD]
controller-1:~$ env | grep OS_PASSWORD
OS_PASSWORD=
controller-1:~$

sysadmin@controller-0:~$ source /etc/platform/openrc
Openstack Admin credentials can only be loaded from the active controller.
controller-0:~$ system application-list
You must provide a password via either --os-password or via env[OS_PASSWORD]
controller-0:~$ system host-list
You must provide a password via either --os-password or via env[OS_PASSWORD]
controller-0:~$ env | grep OS_PASSWORD
OS_PASSWORD=
controller-0:~$

