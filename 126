Good — now your DNS is actually fine 👍
You have both:

✅ IPv4 → 72.2.0.34 (this is what OCP will use)

⚠️ IPv6 → can be ignored (not blocking)


👉 So DNS is NOT your problem anymore


---

🚨 Real Issue (Still the Same)

This line is the smoking gun:

Using internal constant for release image quay.io/openshift-release-dev

👉 Which means:

❌ Installer is STILL using internet
❌ Your mirror is NOT being used
❌ Your ISO will FAIL in disconnected env


---

🧠 Why This Keeps Happening

Because you are running:

openshift-install agent create image

👉 WITHOUT telling installer to use your mirror


---

✅ FINAL FIX (Do EXACTLY this)

🔴 Run this command (don’t skip)

openshift-install agent create image \
  --dir=. \
  --log-level debug \
  --release-image=mirror1.cl2-mno1-rvt.lan:8443/ocp4/openshift4:4.18.23-x86_64


---

⚠️ Important: Why your current run fails

Even though:

Mirror exists ✅

DNS works ✅


👉 Installer default behavior:

Always uses quay unless overridden


---

✅ After Running Correct Command

You should see:

Using release image mirror1.cl2-mno1-rvt.lan:8443

👉 NOT quay.io


---

🔍 If It STILL Shows quay (then check this)

1. Image exists?

skopeo list-tags docker://mirror1.cl2-mno1-rvt.lan:8443/ocp4/openshift4 | grep 4.18.23


---

2. Login works?

podman login mirror1.cl2-mno1-rvt.lan:8443


---

3. Pull secret includes mirror?

Check:

cat install-config.yaml

Must contain:

"mirror1.cl2-mno1-rvt.lan:8443"


---

4. Registry cert trusted?

If self-signed:

additionalTrustBundle: |
  -----BEGIN CERTIFICATE-----


---

🏆 Final Answer

👉 Your setup is correct now (DNS + mirror)
👉 Only missing piece:

✔ You must pass --release-image

Without that: 👉 Installer will ALWAYS go to quay ❌


---

👍 Next Step (if you want)

I can give you:

✅ Fully working install-config.yaml (agent + disconnected)
✅ Validation checklist before booting nodes
✅ Troubleshooting after ISO boot

Just say 👍
