Your pod is stuck in Pending → PodInitializing, but the real root cause is network failure, not the containers themselves.


---

🔥 Actual Root Cause (from Events)

This is the key error:

failed to create pod network sandbox
...
ovn-kubernetes: failed to send CNI request:
dial unix /var/run/ovn-kubernetes/cni//ovn-cni-server.sock: connect: connection refused

👉 This clearly means:

➡️ OVN-Kubernetes CNI is NOT running or not reachable on node
➡️ So Multus cannot attach networks
➡️ So pod sandbox creation fails
➡️ So init containers never start → Pod stays in PodInitializing


---

🧠 What is failing here

Your networking chain:

Pod → Multus → OVN-Kubernetes (default network) → SR-IOV networks

Failure is happening at:

👉 Multus → OVN-Kubernetes socket communication


---

🚨 Key Observations

1. Pod scheduled successfully

Node: master1.airtelvvt.samsungbsc.local

✔ Scheduler is fine


---

2. Init containers NOT starting

Reason: PodInitializing
containers with incomplete status: [sleepy install]

✔ Because pod sandbox itself not created


---

3. OVN socket error (CRITICAL)

/var/run/ovn-kubernetes/cni/ovn-cni-server.sock: connect: connection refused

💥 Means:

ovn-kubernetes-node daemon is DOWN

OR socket not created

OR kubelet can't reach it



---

🛠️ What to check (Step-by-step)

✅ 1. Check OVN pods on that node

oc get pods -n openshift-ovn-kubernetes -o wide | grep master1

👉 Look for:

ovnkube-node

ovnkube-control-plane


✔ Must be Running


---

❌ If NOT running → describe it

oc describe pod <ovnkube-node-pod> -n openshift-ovn-kubernetes


---

✅ 2. Check OVN CNI socket on node

SSH to node:

ls -l /var/run/ovn-kubernetes/cni/

👉 Expected:

ovn-cni-server.sock

❌ If missing → OVN not started properly


---

✅ 3. Check kubelet + OVN logs

journalctl -u kubelet -f

oc logs <ovnkube-node-pod> -n openshift-ovn-kubernetes

Look for:

CNI init failure

ovs issues

permission/socket errors



---

✅ 4. Check node status

oc get nodes

oc describe node master1.airtelvvt.samsungbsc.local

Check:

NetworkUnavailable

Ready condition



---

✅ 5. Restart OVN (quick recovery)

If OVN pod stuck:

oc delete pod -n openshift-ovn-kubernetes <ovnkube-node-pod>

OR restart kubelet (on node):

systemctl restart kubelet


---

✅ 6. Check SR-IOV (secondary issue possibility)

Your pod requests:

openshift.io/pci_sriov_net_bh0c: 2
openshift.io/pci_sriov_net_bh0c_1: 2

Even though current failure is OVN, also verify:

oc get sriovnetworknodestate -n openshift-sriov-network-operator


---

🎯 Final Diagnosis

👉 Primary issue:

> ❌ OVN-Kubernetes CNI is not running / socket unavailable on node master1




---

💡 Why this blocks everything

Because in OpenShift:

Every pod MUST attach default network (OVN) first

Multus cannot proceed without it

So even SR-IOV networks won't attach



---

✅ Quick Fix Summary

1. Check ovnkube-node pod → must be Running


2. Verify /var/run/ovn-kubernetes/cni/ovn-cni-server.sock


3. Restart OVN pod or kubelet


4. Recreate your pod




---

If you want, I can help you pinpoint exactly why OVN is down—just share:

oc get pods -n openshift-ovn-kubernetes -o wide

and I’ll go deeper.
