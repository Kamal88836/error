Perfect — now we have clear diagnosis 🔥


---

🚨 Root Cause (Confirmed)

This line is the key:

status=126

👉 In Podman/containers:

> 126 = permission denied / cannot execute container entrypoint




---

🎯 In your setup, this is caused by:

✅ 1. SELinux blocking volumes (:Z)

✅ 2. Corrupted / partially loaded Quay image

✅ 3. Podman storage inconsistency after manual cleanup


---

🚨 Also important clue

skipping: Loading Quay image archive

👉 Means:

> ❌ quay image NOT properly loaded from bundle




---

✅ FINAL FIX (DO THIS CLEAN — NO SHORTCUTS)


---

🔥 STEP 1: FULL RESET (clean everything)

systemctl stop quay-app quay-pod redis
podman system reset -f

rm -rf /root/quay-install
rm -rf /var/lib/containers/storage/*
rm -rf /var/lib/containers/cache/*
rm -rf /var/lib/registry/*


---

🔥 STEP 2: Fix SELinux (CRITICAL)

setenforce 0

👉 If this fixes → later we’ll make permanent rule


---

🔥 STEP 3: Verify bundle files (VERY IMPORTANT)

ls -lh ~/mirror

You MUST see:

execution-environment.tar
image-archive.tar

👉 If image-archive.tar is missing or corrupt → reinstall will fail


---

🔥 STEP 4: Manually load images (to avoid skip issue)

cd ~/mirror

tar -xf image-archive.tar

podman load -i quay.tar
podman load -i redis.tar
podman load -i pause.tar


---

🔥 STEP 5: Verify images loaded

podman images | grep quay

You should see:

registry.redhat.io/quay/quay-rhel8:v3.12.x


---

🔥 STEP 6: Reinstall mirror registry

./mirror-registry install \
  --initUser sysadmin \
  --initPassword samsung123 \
  --quayHostname mirror1.cl2-mno1-rvt.lan


---

🚨 If still fails (VERY IMPORTANT DEBUG)

Run immediately:

podman logs quay-app

OR:

journalctl -u quay-app -xe


---

🎯 Expected Success

INFO Quay is ready at:
https://mirror1.cl2-mno1-rvt.lan:8443


---

🔥 If SELinux was the issue

After success, re-enable properly:

setenforce 1

Then fix labels:

semanage fcontext -a -t container_file_t "/root/quay-install(/.*)?"
restorecon -Rv /root/quay-install


---

💬 Summary (your exact problem)

Issue	Status

Quay container exit 126	❌
Image not loaded	❌
SELinux blocking	❌
Clean reinstall	✅ (needed)



---

You’re very close — this is the last blocker before full disconnected install works 🚀


---

👉 If it still fails, paste podman logs quay-app — I’ll pinpoint instantly.
