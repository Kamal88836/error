Now you finally have the strongest direct evidence.

The critical lines are:

libceph: mon session lost
ceph: mds0 caps stale
ceph: mds0 reconnect denied
ceph: mds0 session blocklisted
ceph: mds0 rejected session

This proves the CephFS client on worker02 lost connectivity to Ceph and got evicted/blocklisted by MDS.

Most important timeline:

Time	Event

02:40:55	MON session lost
02:41:35	MDS caps stale
02:42:39	MON sessions re-established
02:44:14–15	MDS reconnect denied + blocklisted
02:59:15	further MDS socket close


This is a very classic CephFS client network interruption pattern.

Now combine this with your earlier logs:

Earlier evidence:

Reset indication received from the PF
VF is now untrusted
bond enslaving
VF trusted/untrusted transitions

MDS evidence:

Evicting (and blocklisting) client session

Kernel evidence:

mon session lost
mds reconnect denied
session blocklisted

That is enough for a solid RCA.

The likely sequence is:

1. SR-IOV VF/PF instability or reconfiguration occurred


2. bond interfaces temporarily disrupted traffic


3. Ceph MON/MDS connectivity interrupted


4. CephFS client caps became stale


5. MDS evicted and blocklisted clients


6. applications experienced hangs/probe failures afterward



Also important:

no RocksDB stalls

no OSD corruption

no CRC errors

no packet drop counters

no MON crash evidence


So this does NOT currently point to:

Ceph storage corruption

disk failure

Ceph DB issue


It points much more toward:

transient network interruption

SR-IOV VF reset/trust issue

PF/VF control-plane instability

possibly bond failover instability


Your next verification steps should be:

oc get pods -A -o wide | grep worker02

oc logs -n openshift-sriov-network-operator -l app=sriov-network-config-daemon --since=12h

oc logs -n openshift-multus ds/multus --since=12h

ip -d link show bh0
cat /proc/net/bonding/bh0

Especially investigate:

why VF trust toggling happened repeatedly

whether sriov-network-config-daemon reconfigured VFs

whether bond mode changes were attempted dynamically

whether PF firmware/driver mismatch exists (ice + iavf)
