This gives a much clearer picture now.

Important findings:

1. Ceph issue definitely happened You already proved:



mon session lost
mds caps stale
reconnect denied
session blocklisted

That confirms storage connectivity interruption.


---

2. Multus/API connectivity issue also happened simultaneously Critical evidence:



http2: client connection lost
Failed to watch *v1.Pod
Failed to watch *NetworkAttachmentDefinition

at:

02:43:06

This is extremely important because it means:

not only Ceph traffic

but also Kubernetes API traffic


was interrupted from the node.

That strongly points to node networking instability rather than Ceph-only issue.


---

3. SR-IOV daemon currently shows no active reconfiguration Current logs show only polling/status checks:



Interfaces not changed

So the disruptive event already passed.


---

4. bh0 does not exist now This means:



either the bond interface existed only inside pods/netns

or interface naming changed

or earlier logs referencing bond/VF events were from another interface/node


Need to identify actual active interfaces on worker02.

Run:

ip link

and:

nmcli con show

and:

ip addr


---

5. Very important systemd failures exist



You have:

NetworkManager-wait-online.service FAILED
systemd-network-generator.service FAILED

This is highly suspicious in a node already showing:

Ceph disconnects

API watch disconnects

VF trust/reset history


This strengthens the networking RCA.


---

Current strongest RCA hypothesis:

Component	Evidence

CephFS disruption	MDS evict/blocklist
Node network interruption	API watch lost
Possible SR-IOV/VF instability	earlier VF reset/trust logs
Possible node network stack instability	NetworkManager failures
Storage backend corruption	NO evidence
OSD failure	NO evidence


So at this point the issue is looking more like:

> transient node-level network disruption on worker02 causing CephFS session eviction and Kubernetes API disconnects



rather than a Ceph storage fault.

Most useful next commands:

systemctl status NetworkManager-wait-online.service

systemctl status systemd-network-generator.service

journalctl -u NetworkManager --since "2026-05-19 02:35:00" --until "2026-05-19 02:50:00"

dmesg -T | egrep -i 'ice|iavf|reset|vf|link.*down|link.*up'

ip link

I would especially focus on:

Intel ice/iavf

SR-IOV VF resets

interface flaps

bond failover

NetworkManager reload/reconcile events


because the timestamps across Ceph + Multus strongly align with network interruption.
