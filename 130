Good, you’ve gone deep enough now — and this error clearly shows why this path is painful in your environment.


---

❌ Why your pip install nmstate failed

From your logs:

ERROR: Dependency lookup for cairo failed
pkg-config not found
cmake not found

👉 Root cause:

nmstatectl depends on PyGObject

PyGObject depends on:

cairo

pkg-config

cmake

system libraries



👉 These are native OS packages, not Python-only


---

🚫 Why this WON’T work in your setup

You are:

❌ Disconnected / limited DNS

❌ Missing system packages (pkg-config, libcairo, etc.)

❌ Cannot install dependencies via apt


👉 So pip build will always fail


---

✅ Final Practical Answer (What you SHOULD do)

👉 Stop trying to install nmstate — not needed


---

🚀 Use this instead (correct approach for your case)

export NMSTATECTL_PATH=/bin/true

Then:

./openshift-install agent create cluster-manifests


---

🧠 Why this is the RIGHT solution

Agent-based installer only uses nmstate for validation

Actual network config is applied on node during boot

In disconnected environments → this step is commonly skipped



---

⚠️ But now VERY IMPORTANT

Since you are bypassing validation, your config must be correct.


---

🔍 Check these 3 things (critical)

1. Interface name (ESXi default)

Run inside VM or check template:

ip a

👉 Most likely:

ens192   ✅

NOT:

eth0 ❌


---

2. MAC address must match EXACT

From ESXi:

VM → Edit Settings → NIC → copy MAC


Match:

interfaces:
  - name: ens192
    macAddress: XX:XX:XX:XX:XX:XX


---

3. Static IP config correct

ipv4:
  enabled: true
  dhcp: false


---

🔥 What you should do NOW

Run this:

export NMSTATECTL_PATH=/bin/true

./openshift-install agent create cluster-manifests
./openshift-install agent create image


---

📌 Reality check (important)

In real production:

nmstate is installed via repo or mirror


But in your case:

ESXi lab

disconnected

SNO


👉 Skipping nmstate validation is normal and widely used


---

👍 If you want next step help

Paste your:

agent-config.yaml

I’ll:

Fix interface name (very common issue)

Validate IP config

Ensure your SNO boots without network failure 🚀
