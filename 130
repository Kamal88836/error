Alright — now we finally have the complete picture and I’ll be very direct:

👉 Your scheduler is NOT recovering because revision-6 itself is corrupted.
Deleting pods won’t help — Kubernetes keeps recreating the same broken revision.


---

🔴 FINAL ROOT CAUSE (CONFIRMED)

From your output:

revision=6
scheduler cache is corrupted

👉 This means:

Static pod config (/etc/kubernetes/static-pod-resources/...-6) is bad

kubelet keeps restarting the SAME broken revision

So it will never recover until a new revision is forced



---

❗ WHY IT’S STILL FAILING

You did NOT run this step yet (critical):

oc patch kubeScheduler cluster ...

👉 Without this:

Cluster keeps using revision-6

Which is already corrupted



---

✅ 🔥 REAL FIX (DO THIS NOW)

🧨 STEP 1: Force NEW scheduler revision (MOST IMPORTANT)

oc patch kubeScheduler cluster \
  --type=merge \
  -p '{"spec":{"forceRedeploymentReason":"scheduler-recovery-2"}}'


---

🔍 STEP 2: Verify new revision

oc get pods -n openshift-kube-scheduler -o wide

👉 You MUST see:

revision=7 (or higher)

⚠️ If still revision=6 → patch didn’t apply


---

🧨 STEP 3: Restart kubelet ONLY on master1

ssh core@master1
sudo systemctl restart kubelet

👉 This will:

Load new revision

Drop corrupted scheduler cache



---

🧨 STEP 4: Uncordon nodes (IMPORTANT)

Right now:

master1 → SchedulingDisabled
master3 → SchedulingDisabled

👉 That’s why pods are Pending

Fix:

oc adm uncordon master1
oc adm uncordon master3


---

🧨 STEP 5: Clean stuck pods (optional but recommended)

oc delete pod -n cnf-bscf --all --force --grace-period=0


---

🔍 EXPECTED RESULT

After 2–3 minutes:

oc get pods -n openshift-kube-scheduler

✅ Should be:

master1 → 3/3 Running ✅


---

oc get co

✅ Should be:

kube-scheduler → Available=True, Degraded=False


---

⚠️ IMPORTANT OBSERVATION (NEW)

Your CPU situation changed:

Node	CPU

master1	7% ✅
master2	98% 🔥
master3	12% ✅


👉 Now master2 is overloaded


---

💥 WHY YOUR APPS STILL PENDING

Because:

master1 + master3 → cordoned ❌

master2 → overloaded ❌


👉 Result:

No node available → Pending pods


---

✅ AFTER FIX (DO THIS)

Either:

✔ Uncordon all:

oc adm uncordon master1
oc adm uncordon master3


---

OR reduce load on master2:

oc adm top pods -A | sort -k3 -nr | head


---

🚀 FINAL SUMMARY

✔ Problem = corrupted scheduler revision (rev-6)
✔ Fix = force new revision (rev-7)
✔ Secondary issue = nodes cordoned + CPU imbalance


---

👉 NEXT STEP (IMPORTANT)

Run this NOW and paste output:

oc get pods -n openshift-kube-scheduler -o wide

👉 I’ll confirm if revision changed and guide final stabilization 👍
