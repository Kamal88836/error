Great — this output changes things (and saves you from a wrong step) 👍


---

🔴 Important Correction

Earlier we thought MON DB was missing — but now we see:

/var/lib/ceph/mon/
├── ceph-controller        ✅
├── store.db               ✅ (exists)

👉 So:

❗ MON DB is NOT missing

👉 It is corrupted / inconsistent


---

🚨 Why your rebuild command failed earlier

You ran:

ceph-monstore-tool /var/lib/ceph/mon rebuild

👉 That path is WRONG for StarlingX

Actual MON path is:

/var/lib/ceph/mon/ceph-controller


---

✅ Correct Fix (Now we do it properly)

🔥 Step 1: Rebuild MON using correct path

On controller-1:

sudo ceph-monstore-tool /var/lib/ceph/mon/ceph-controller rebuild


---

🔥 Step 2: Restart SM

sudo systemctl restart sm


---

⏳ Step 3: Wait 2–3 minutes

Then check:

sm-dump | grep ceph-mon


---

✅ Expected Result

ceph-mon enabled-active enabled-active


---

🔥 Step 4: Validate Ceph

sudo ceph -s

👉 Should NOT hang


---

🔥 Step 5: Check controller-services

sm-dump | grep controller-services

👉 Should become:

active active


---

⚠️ DO NOT DO THIS

❌ Do NOT delete /var/lib/ceph/mon/*
👉 You already have usable MON DB


---

🧠 Why this works

MON DB exists but inconsistent (after HA chaos)

Rebuild reconstructs:

monmap

paxos state


Allows MON to start cleanly



---

🚀 You are at FINAL FIX

👉 Run now:

sudo ceph-monstore-tool /var/lib/ceph/mon/ceph-controller rebuild


---

Then send:

sm-dump | grep -E "ceph-mon|controller-services"


---

This should finally stabilize your cluster 👍
