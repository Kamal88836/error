Perfect — now focus only on controller-1. I’ll give you exact steps in order 👇


---

🎯 Goal

Make cluster stable on single controller (controller-1)


---

🚨 Current issue

ceph-mon → enabling-throttle ❌
controller-services → go-active ❌

👉 Means MON still not starting properly


---

✅ DO THIS STEP-BY-STEP (on controller-1)


---

🔥 Step 1: Clean Ceph runtime (IMPORTANT)

sudo rm -rf /var/run/ceph/*
sudo rm -rf /var/log/ceph/*


---

🔥 Step 2: Fix permissions

sudo chown -R root:root /var/lib/ceph/mon


---

🔥 Step 3: Restart SM (core step)

sudo systemctl restart sm


---

⏳ Step 4: Wait 2–3 minutes


---

🔍 Step 5: Check MON status

sm-dump | grep ceph-mon

👉 Expected:

enabled-active enabled-active


---

🔍 Step 6: Check if MON container started

crictl ps | grep ceph-mon

👉 Must show container


---

🔍 Step 7: Check Ceph health

sudo ceph -s

👉 Should NOT hang


---

🚨 If STILL stuck (very important)

Run this and send output:

sudo tail -n 100 /var/log/platform.log | grep -i ceph

👉 This will show exact failure reason


---

⚠️ DO NOT

❌ Do NOT power ON controller-0
❌ Do NOT delete ceph data again
❌ Do NOT reboot now


---

🧠 What we already fixed

✔ MON DB rebuilt
✔ monmap recreated
✔ cluster identity OK

👉 Now only MON startup pending


---

🚀 Your action NOW

Run:

sudo rm -rf /var/run/ceph/*
sudo rm -rf /var/log/ceph/*
sudo systemctl restart sm

Then send:

sm-dump | grep ceph-mon
crictl ps | grep ceph-mon


---

We are at final step — this will bring cluster back 👍
