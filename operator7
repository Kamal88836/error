Now it’s 100% clear what’s happening — and this is a known disconnected OLM issue, not your mistake.


---

🚨 Root Cause (FINAL)

👉 Subscription created but:

oc get installplan → EMPTY ❌

👉 That means:

💥 Catalog does NOT contain installable bundle for rook-ceph-operator


---

🧠 Why This Happens

Even though you see:

oc get packagemanifest
→ rook-ceph-operator ✔

👉 That only means metadata exists

❌ It does NOT mean:

bundle image exists in mirror

related images are mirrored



---

🔥 REAL PROBLEM

👉 Your mirror is missing:

rook-ceph operator bundle + related images ❌

So OLM cannot:

create InstallPlan ❌

install operator ❌



---

✅ PROOF (Run This)

oc get packagemanifest rook-ceph-operator -n openshift-marketplace -o yaml | grep -A5 currentCSV

👉 If it shows version but no install → mirror incomplete


---

🧠 Why It Happened

Your earlier ImageSet:

- name: odf-operator
- name: ocs-operator
- name: odf-csi-addons-operator

👉 This is NOT enough in disconnected env ❌

👉 Missing:

rook-ceph-operator

mcg-operator

cephcsi-operator

etc



---

🔥 ONLY REAL FIX (No Shortcut)

👉 You MUST re-mirror operators properly


---

✅ CORRECT ImageSet (Minimal but COMPLETE)

apiVersion: mirror.openshift.io/v2alpha1
kind: ImageSetConfiguration

mirror:
  operators:
    - catalog: registry.redhat.io/redhat/redhat-operator-index:v4.19
      packages:

        # ODF FULL STACK (MANDATORY)
        - name: odf-operator
          channels:
            - name: stable-4.19

        - name: ocs-operator
          channels:
            - name: stable-4.19

        - name: mcg-operator
          channels:
            - name: stable-4.19

        - name: rook-ceph-operator
          channels:
            - name: stable-4.19

        - name: cephcsi-operator
          channels:
            - name: stable-4.19

        - name: odf-csi-addons-operator
          channels:
            - name: stable-4.19

        - name: odf-prometheus-operator
          channels:
            - name: stable-4.19

        - name: odf-dependencies
          channels:
            - name: stable-4.19

        - name: recipe
          channels:
            - name: stable-4.19

  helm: {}


---

🚀 What You Must Do

Step 1: Mirror again

oc mirror -c imageset-config.yaml \
  file:///root/mirror_fixed \
  --v2


---

Step 2: Push to registry

oc mirror -c imageset-config.yaml \
  --from file:///root/mirror_fixed \
  docker://bastion.srib-lab-ocp.lan:8443 \
  --v2


---

Step 3: Reapply catalog

oc apply -f cs-redhat-operator-index-v4-19.yaml


---

Step 4: Restart marketplace

oc delete pod -n openshift-marketplace --all


---

Step 5: Recreate subscription

oc delete subscription rook-ceph-operator -n openshift-storage

Recreate again.


---

🎯 Expected Result

oc get installplan -n openshift-storage

👉 Now you WILL see:

install-xxxxx ✅


---

⚠️ Important Truth

👉 In disconnected env:

> If InstallPlan is NOT created → mirror is incomplete




---

🧠 Final Summary

Problem	Reason

No InstallPlan	Missing operator images
No ServiceAccount	Operator not installed
Pods failing	Missing SA



---

🚀 Next Step

If you want, I can:

✔ Verify your current mirror (quick check)
✔ Give smallest possible working mirror (optimized)
✔ Help avoid full re-download

Just tell 👍
