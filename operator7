Alright — now I see the exact state clearly. You’re very close, but this is a classic ODF broken ownership case after subscription deletion.

Let me be very direct:


---

🚨 What’s Actually Broken

You have:

✔ odf-operator running
✔ Ceph pods running (partially)
❌ rook-ceph operator is NOT installed
❌ ServiceAccounts missing

👉 That’s why pods fail:

serviceaccounts "rook-ceph-default" not found


---

🧠 Critical Insight (This is the key)

👉 ODF is NOT recreating dependencies automatically anymore

Because:

Ownership chain is broken

OLM doesn’t know what to reconcile


👉 So waiting will NOT fix this ❌


---

🔥 Correct Recovery Approach (WORKING METHOD)

We must manually restore dependency operators once, then ODF will take over again.


---

✅ Step 1: Install rook-ceph operator (but correctly)

👉 Earlier attempt didn’t work because install didn’t trigger.

Run this (force install):

oc delete installplan -n openshift-storage --all

Then reapply:

cat <<EOF | oc apply -f -
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rook-ceph-operator
  namespace: openshift-storage
spec:
  channel: stable-4.19
  name: rook-ceph-operator
  source: cs-redhat-operator-index-v4-19
  sourceNamespace: openshift-marketplace
  installPlanApproval: Automatic
EOF


---

🔄 Step 2: Watch InstallPlan

oc get installplan -n openshift-storage

👉 If not progressing, approve manually:

oc patch installplan <name> -n openshift-storage \
  --type merge -p '{"spec":{"approved":true}}'


---

🔄 Step 3: Watch CSV

watch oc get csv -n openshift-storage

👉 MUST appear:

rook-ceph-operator.v4.19.x → Succeeded


---

🔄 Step 4: Verify ServiceAccounts

oc get sa -n openshift-storage | grep rook

👉 Expected:

rook-ceph-default ✅
rook-ceph-system ✅


---

🔄 Step 5: Restart Ceph Pods

oc delete pod -n openshift-storage \
  -l app=rook-ceph-mon

oc delete pod -n openshift-storage \
  -l app=rook-ceph-osd


---

🚀 Expected Final State

oc get po -n openshift-storage

👉 All should become:

Running ✅


---

⚠️ Important (Don’t skip)

👉 This is one-time manual recovery

After this:

ODF regains control ✔

Future upgrades automatic ✔



---

🧠 Why Previous Attempt Failed

You created subscription, but:

Old InstallPlan stuck ❌

OLM didn’t trigger install ❌


👉 Deleting InstallPlan forces fresh resolution


---

🚀 Do This Now

Run:

oc get installplan -n openshift-storage
oc get csv -n openshift-storage

👉 Send output — I’ll verify if installation triggered correctly 👍
