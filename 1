Your cluster is already on OpenShift 4.18.17, but the ClusterVersion object is still reporting Progressing=True and Failing=True because one of the cluster operators is unhealthy:

Failing=True
Reason: ClusterOperatorNotAvailable
Message: Cluster operator monitoring is not available

Progressing=True
Message: Unable to apply 4.18.17: the cluster operator monitoring is not available

The upgrade itself completed successfully:

History:
Version: 4.18.17
State: Completed
Verified: true
Completion Time: 2025-11-18T20:16:11Z

First thing to check

oc get co

Look for:

oc get co monitoring

and share:

oc describe co monitoring

Also check:

oc get pods -n openshift-monitoring
oc get pods -n openshift-user-workload-monitoring

Pay attention to any pods in:

CrashLoopBackOff

Pending

ImagePullBackOff

Error



---

I also see two upgrade blockers

1. Kube Controller Manager unsupported override

UnsupportedConfigOverridesUpgradeable:
setting: [extendedArguments.terminated-pod-gc-threshold.0]

Check:

oc get kubecontrollermanager cluster -o yaml

Look for:

unsupportedConfigOverrides:

or

extendedArguments:
  terminated-pod-gc-threshold:

This will block future minor upgrades.


---

2. MachineConfigPool updating

machine-config should not be upgraded:
PoolUpdating

Check:

oc get mcp

All pools should show:

UPDATED=True
UPDATING=False
DEGRADED=False

If not, describe the affected MCP:

oc describe mcp <pool-name>


---

Since this cluster previously had ODF

Your upgrade history shows:

odf-operator.v4.14.22-rhodf is incompatible with OpenShift minor versions greater than 4.15

You were able to force through that blocker and eventually reached 4.18.17, but I'd verify ODF health as well:

oc get csv -A | grep -i odf
oc get pods -n openshift-storage
oc get storagecluster -A


---

Most important command right now

Run:

oc get co

and paste the output. That will immediately tell us whether the issue is only the monitoring operator or if there are additional degraded operators keeping the ClusterVersion in a failing state.
