This is excellent information. It changes my confidence level.

Good news

The node itself is now healthy:

✅ MCP:

Updated=True
Updating=False
Degraded=False

✅ Node:

Ready

✅ MachineConfig state:

machineconfiguration.openshift.io/state: Done

✅ Current and Desired MachineConfig are identical.

✅ Kubernetes has the correct addresses registered:

InternalIP: 72.1.1.25
InternalIP: 2405:100:200:72::1:25

✅ OVN also has the correct primary interface:

node-primary-ifaddr:
IPv4: 72.1.1.25
IPv6: 2405:100:200:72::1:25


So OpenShift is no longer using the incorrect IPv6 address.


---

But I noticed two things

1. Your Master MCP is paused again

Your oc describe mcp master shows:

Spec:
  Paused: true

and the Machine Config operator reports:

machine-config
Degraded=True

Failed to resync 4.18.23 because:
the required MachineConfigPool master was paused

Earlier in the conversation you unpaused it. Now it is paused again.

Please verify:

oc get mcp master -o jsonpath='{.spec.paused}'; echo

If it prints:

true

and you do not intentionally want it paused, unpause it:

oc patch mcp master --type=merge -p '{"spec":{"paused":false}}'

After a minute:

oc get co machine-config

The degraded message should disappear.


---

2. Your node is cordoned

Ready,SchedulingDisabled

and

Unschedulable: true

If this is not intentional, uncordon it:

oc adm uncordon sno1.lab1.ocp.lan


---

Regarding the original CRI-O issue

After reviewing everything you've provided, my conclusion is:

Your networking configuration is fundamentally correct.

Kubernetes knows the correct IPv4 and IPv6 addresses.

OVN knows the correct addresses.

20-nodenet.conf is currently correct.

The bad IPv6 address never actually existed on the node.


That means the original failure was most likely a transient generation problem during the MachineConfig reboot sequence, not a permanent networking misconfiguration.


---

One thing I would still fix

Your .nmconnection file still contains:

dns=22405:100:200:72::1:17

This is invalid.

It should be:

dns=2405:100:200:72::1:17

Although this wasn't the cause of the CRI-O failure, it's a configuration error that should be corrected.


---

My recommendation

I would not make any more changes related to CRI-O or kubelet now because they are working correctly.

Instead:

1. Fix the invalid IPv6 DNS entry.


2. Ensure the Master MCP is only paused if that is intentional.


3. Uncordon the node if it should schedule workloads.


4. Observe the next planned reboot.



If the next reboot again generates a 20-nodenet.conf with a nonexistent IPv6 address, then I would consider it a reproducible OpenShift issue and collect logs for a Red Hat support case.

One question: Did you intentionally pause the Master MCP and cordon the SNO node for SR-IOV or maintenance, or did those happen automatically?
