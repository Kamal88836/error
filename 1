Now we can see the ODF operator pod correctly:

odf-operator-controller-manager-79cdcbd66f-vdk9t

Restart it once explicitly:

oc delete pod odf-operator-controller-manager-79cdcbd66f-vdk9t -n openshift-storage

Then immediately monitor:

oc logs -f deploy/odf-operator-controller-manager -n openshift-storage

and:

oc get storagecluster -n openshift-storage -w

However, from your pod list there is another very important observation:

Most storage components were recreated recently:

many pods age 5h

some 6h

some 2d

operator restarted recently


This suggests a partial reconciliation already happened earlier.

Now verify whether the newer CSV object actually exists in cluster:

oc get csv -n openshift-storage rook-ceph-operator.v4.19.12-rhodf

and:

oc get csv -n openshift-storage ocs-operator.v4.19.12-rhodf

This is critical.

If those CSV objects do NOT exist:

catalog advertises them

but bundle/image resolution failed

disconnected mirror incomplete


If they DO exist but are Pending/Failed:

OLM resolution problem


If they do not exist at all, then check whether the bundle image is mirrored:

Example:

podman pull --tls-verify=false nwharbor.sec.samsung.net/<path-to-rhodf-bundle>

because disconnected environments often mirror:

index image but miss:

actual operator bundle images


Your cluster currently remains healthy, so avoid deleting subscriptions/CSVs aggressively until we confirm bundle availability.
