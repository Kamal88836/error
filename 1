This output reveals the remaining issue.

Root cause is no longer CRI-O

Your node is healthy:

✅ Node = Ready

✅ MCP = Updated=True

✅ Kubelet and CRI-O are running

✅ MachineConfig applied successfully


The remaining problem is this:

machine-config:
Failed to resync because:
the required MachineConfigPool master was paused

And your MCP shows:

Spec:
  Paused: true

Something is automatically pausing the MCP

Notice this annotation:

Annotations:
  sriovnetwork.openshift.io/state: Paused

This is not a standard MCO annotation.

Because you are using the SR-IOV Network Operator, it is likely managing the MCP and setting it back to Paused. That explains why:

You uncordoned the node.

A few seconds later it became SchedulingDisabled again.

The MCP remains Paused.


This is usually done by the SR-IOV operator during policy application or reboot-required workflows.

Verify this

Run:

oc get sriovoperatorconfig -A -o yaml

and

oc get sriovnetworknodepolicy -A

Also check:

oc get sriovnetworknodestate -A

I especially want to see whether the node state reports RebootRequired or SyncStatus.

From your node description I already noticed:

sriovnetwork.openshift.io/state: Reboot_Required

That is another clue that the SR-IOV Operator still thinks the node requires a reboot or is in a paused state.

Why the node gets cordoned again

The SR-IOV Operator can:

cordon the node,

pause the MCP,

wait for configuration to complete,

uncordon the node when finished.


If it never reaches the final step, the node stays cordoned and the MCP stays paused.

Next commands

Please run:

oc get sriovnetworknodestate -A

and

oc describe sriovnetworknodestate sno1.lab1.ocp.lan -n openshift-sriov-network-operator

I suspect we'll find the SR-IOV operator is what's reapplying the pause and cordon, rather than a Machine Config or CRI-O issue. That will tell us why it is not completing its workflow.
