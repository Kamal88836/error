Your operators are in a mixed but mostly healthy upgraded state.

The important observation is:

Cluster operators like:

local-storage

ptp

sriov

odf-operator



have already moved to 4.19 CSVs.

But most ODF dependency operators are still running 4.18.17-rhodf even though their subscriptions are on stable-4.19.

This is normal behavior in many ODF upgrades because:

odf-operator upgrades first

dependent operators upgrade later based on compatibility gates

OLM waits for dependency resolution and health checks


Current state appears consistent because all CSVs show:

PHASE   Succeeded

which means OLM considers them healthy.

The only slightly unusual thing is this mismatch:

Operator	Subscription Channel	Running CSV

cephcsi	stable-4.19	4.18.17
rook-ceph	stable-4.19	4.18.17
mcg	stable-4.19	4.18.17


That usually means:

either newer CSVs are not yet published in catalog

or upgrade graph intentionally pins them

or upgrade conditions are not yet satisfied


You can verify whether updates are available:

oc describe sub -n openshift-storage odf-operator

and check:

CurrentCSV:
InstalledCSV:
State:

Also verify catalog health:

oc get pods -n openshift-marketplace
oc get packagemanifest | grep -i odf

Check if any CSV is pending:

oc get csv -A | grep -vi succeeded

and installplan issues:

oc describe installplan -n openshift-storage install-rkpq9

For ODF specifically, confirm cluster health before expecting remaining components to move:

oc get storagecluster -n openshift-storage
oc get cephcluster -n openshift-storage

You want:

PHASE=Ready

HEALTH_OK


Also check upgrade conditions:

oc get co

If all operators are healthy and no installplan is pending, then this is likely an expected staged upgrade state rather than a problem.

The odf-operator.v4.19.12-rhodf already being installed is the main indicator that the upgrade path is progressing correctly.
