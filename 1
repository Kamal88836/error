This is the missing piece. The evidence is now very clear.

What we know

Your manual drain succeeds (dry run):

node/sno1.lab1.ocp.lan drained (dry run)

There are:

✅ No terminating pods

✅ No pods blocking eviction

✅ Node is healthy

✅ MCP is updated

❌ SR-IOV still reports:

Sync Status: InProgress
Current State: Draining
Desired State: Reboot_Required

❌ Events repeatedly show:

DrainController
node drain operation was not completed


The strange part

The SR-IOV operator deployment logs are empty:

oc logs -n openshift-sriov-network-operator deploy/sriov-network-operator

returned nothing.

That usually means the deployment isn't emitting logs, or the operator is restarting before logging anything useful.

I think this is an SR-IOV operator state issue

The operator is stuck in this loop:

Pause MCP
      ↓
Cordon node
      ↓
Try drain
      ↓
Never marks drain complete
      ↓
Keeps node cordoned
      ↓
Starts over

Since your manual drain succeeds, the operator's internal state appears to be stuck rather than the node actually being undrainable.

Next check

Let's inspect the config daemon, because it's the component updating the SriovNetworkNodeState.

Run:

oc logs -n openshift-sriov-network-operator sriov-network-config-daemon-d2cwh --tail=200

or, more generally:

oc logs -n openshift-sriov-network-operator -l app=sriov-network-config-daemon --tail=200

I'm looking for messages around:

Draining

DrainController

Reboot_Required

SyncStatus


One more thing to verify

Please also run:

oc get ds -n openshift-sriov-network-operator

and

oc get deploy -n openshift-sriov-network-operator

to confirm all SR-IOV components are fully available.


---

My assessment

At this stage:

The original CRI-O IPv6 bind issue is resolved.

The cluster networking is healthy.

The remaining problem is entirely within the SR-IOV operator workflow.

Specifically, the DrainController never transitions the node from Draining to Reboot_Required (or Succeeded), so it continuously pauses the MCP and re-cordons the node.


This looks much more like an SR-IOV operator state machine issue than a networking or Machine Config issue. The config daemon logs should tell us why it refuses to complete the drain.
