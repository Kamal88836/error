[root@bastion01 OCP_operator]# oc patch noobaa noobaa -n openshift-storage \
> --type=merge \
> -p '{
>   "spec":{
>     "cleanupPolicy":{
>       "allowNoobaaDeletion": true,
>       "confirmation": "yes-really-delete-noobaa"
>     }
>   }
> }'
noobaa.noobaa.io/noobaa patched
[root@bastion01 OCP_operator]# oc get noobaa noobaa -o yaml -n openshift-storage | grep -A10 cleanupPolicy
  cleanupPolicy:
    allowNoobaaDeletion: true
    confirmation: yes-really-delete-noobaa
  coreResources:
    limits:
      cpu: 999m
      memory: 4Gi
    requests:
      cpu: 999m
      memory: 4Gi
  dbImage: registry.redhat.io/rhel9/postgresql-15@sha256:cba1417b7e8a5b55289aa951c48dc940c72ebea5380045f32cd8faba41937f9b
[root@bastion01 OCP_operator]# oc get crd noobaas.noobaa.io -o yaml | grep -A20 cleanupPolicy
              cleanupPolicy:
                description: CleanupPolicy (optional) Indicates user's policy for
                  deletion
                properties:
                  allowNoobaaDeletion:
                    type: boolean
                  confirmation:
                    description: CleanupConfirmationProperty is a string that specifies
                      cleanup confirmation
                    type: string
                type: object
              coreResources:
                description: CoreResources (optional) overrides the default resource
                  requirements for the server container
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
[root@bastion01 OCP_operator]#
