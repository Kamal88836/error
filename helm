Thanks — this helps. Your values file is clean ✅ and does NOT contain inkState.

👉 So the problem is 100% inside the Helm chart templates, not your config.


---

🔴 Final Root Cause (Confirmed)

Your cluster supports linkState ✔️

Your values.yaml doesn’t define inkState ✔️

Yet Helm is trying to apply inkState ❌


👉 Therefore:

The chart bscf-pre:261.0.1 itself is broken (hardcoded wrong field)


---

🔍 What’s happening internally

The chart likely has something like:

spec:
  inkState: {{ .Values.sriov.linkState | default "enable" }}

👉 Instead of:

spec:
  linkState: ...

So even though you didn’t pass it, Helm renders it anyway.


---

🛠️ Fix (Only working solution now)

✅ Step 1: Pull and unpack the chart

helm pull oci://sribharbor.sec.samsung.net/integ-ran/bscf-pre --version 261.0.1
tar -xvf bscf-pre-261.0.1.tgz
cd bscf-pre


---

✅ Step 2: Find the issue

grep -r inkState .

You will see files like:

templates/sriovnetwork.yaml


---

✅ Step 3: Fix it

Replace everywhere:

inkState:

👉 with:

linkState:


---

✅ Step 4: Install from local chart

helm install bscf-pre ./bscf-pre -f local-values-pre.yaml


---

⚠️ Important (Don’t skip)

Do NOT try to fix via values.yaml — it won’t work because:

Field is hardcoded in template

Not controlled by your values



---

🧠 Why this happened

This is usually due to:

Typo by chart developer (inkState vs linkState)

Or copied from incorrect internal schema


👉 Not related to:

OpenShift version ❌

SR-IOV operator ❌

Your config ❌



---

✅ Alternative (if you cannot modify chart)

Ask vendor/team:

👉 “Provide chart compatible with OpenShift SR-IOV CRD (linkState, not inkState)”


---

🚀 Quick sanity check (optional)

After fix, verify:

helm template ./bscf-pre -f local-values-pre.yaml | grep linkState

👉 Should show:

linkState: enable


---

✅ Final Summary

Component	Status

Your cluster	✅ Correct
Your values.yaml	✅ Correct
Helm chart	❌ Bug
Fix	🔧 Patch template



---

If you want, paste one rendered SriovNetwork YAML, I can double-check nothing else will break after this fix.
