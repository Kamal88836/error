Perfect — this output gives the exact root cause 🎯 (no guessing now).


---

🔴 FINAL ISSUE (Confirmed)

From Events:

pod has unbound immediate PersistentVolumeClaims

👉 Your pods are NOT scheduling because PVCs are NOT bound


---

📦 What’s happening

Your pod requires:

ClaimName:
- bscf236-log-pvc
- bscf236-shared-pvc

But earlier you showed:

kubectl get pv
STATUS: Released ❌

👉 That means:

Old PVs exist

But they are NOT bound to new PVCs

Kubernetes cannot attach storage

So pods stay Pending



---

🔴 Why PV = Released is a problem

Released ≠ Available

👉 Released means:

Previously used by old PVC

Still has old claim reference

Kubernetes will NOT reuse automatically



---

✅ Fix (IMPORTANT – follow exactly)


---

✅ Step 1: Check PVC status

kubectl get pvc

👉 You will see:

Pending ❌


---

✅ Step 2: Delete old PVCs (safe if fresh deployment)

kubectl delete pvc bscf236-log-pvc
kubectl delete pvc bscf236-shared-pvc
kubectl delete pvc bscf236-db-pvc


---

✅ Step 3: Fix PVs (make them reusable)

For each PV:

kubectl patch pv <pv-name> -p '{"spec":{"claimRef": null}}'

Example:

kubectl patch pv pvc-99d6cfc9-a2e6-4271-8927-f308775a0bbd -p '{"spec":{"claimRef": null}}'
kubectl patch pv pvc-a22f03a8-dbc9-454e-8c47-196e5cc6b719 -p '{"spec":{"claimRef": null}}'
kubectl patch pv pvc-ef5246cb-11d7-40ad-ac92-1156d35dc7b7 -p '{"spec":{"claimRef": null}}'


---

✅ Step 4: Verify PV becomes Available

kubectl get pv

Expected:

STATUS: Available ✅


---

✅ Step 5: Recreate PVCs

Just re-run Helm:

helm upgrade bscf oci://sribharbor.sec.samsung.net/rel-ran/bscf -f local-values-25A.yaml


---

✅ Step 6: Verify binding

kubectl get pvc

Expected:

STATUS: Bound ✅


---

✅ Step 7: Pods will schedule

kubectl get po

👉 Now pods should move from:

Pending → ContainerCreating → Running


---

🚨 Important Note (WRCP)

This issue happened because:

You had old BSCF deployment

PVs were reused

WRCP uses CephFS

PVC lifecycle was not cleaned properly



---

💡 Key takeaway

👉 Your issue is NOT:

❌ CPU

❌ HugePages

❌ DRBD


👉 It is:

> 🔥 Storage (PVC not bound due to Released PVs)




---

🚀 After this fix

If pods still Pending → THEN we check:

hugepages

CPU isolation

SR-IOV


But first fix storage (this is blocking everything)


---

👉 Do this and tell me output:

kubectl get pvc
kubectl get pv

I’ll confirm everything is clean 👍
